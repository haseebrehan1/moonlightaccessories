# Deploy artifacts run on Linux. If CRLF ever reaches the server, bash fails
# with "\r: command not found" and systemd/nginx silently mis-parse values.
*.sh            text eol=lf
*.service       text eol=lf
*.conf          text eol=lf
deploy/**       text eol=lf
